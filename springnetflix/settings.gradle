rootProject.name = 'springnetflix'
include 'app1'
include 'app2'
include 'reg-server'
include 'cfg-server'
include 'api-gateway'
include 'swagger2'

rootProject.children.each{
  it.name="${rootProject.name}-${it.name}"
}
