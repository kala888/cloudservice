version: '2'
services:
  rabbitmq:
    ports:
      - 5672:5672

  config-service:
    build: config-service
    ports:
      - 7100:7100

  gateway:
    build: gateway

  a-service:
    build: a-service
    ports:
      - 5100:5100

  b-service:
    build: b-service
    ports:
      - 5200:5200

  consul:
    build: consul
  # hystrix-dashboard:
  #   build: hystrix-dashboard

  admin-dashboard:
    build: admin-dashboard
