security:
  oauth2:
    client:
      clientId: b-service
      clientSecret: ${B_SERVICE_PASSWORD}
      accessTokenUri: http://auth-service:5000/oauth/token
      grant-type: client_credentials
      scope: server
#
# spring:
#   data:
#     mongodb:
#       host: b-mongodb
#       username: ${MONGODB_USER}
#       password: ${MONGODB_PASSWORD}
#       database: ${MONGODB_DATABASE}
#       port: 27017

server.port: 5200
# management.port: 5201
