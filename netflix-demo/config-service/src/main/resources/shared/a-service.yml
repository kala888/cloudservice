security:
  oauth2:
    client:
      clientId: a-service
      clientSecret: ${A_SERVICE_PASSWORD}
      accessTokenUri: http://auth-service:5000/oauth/token
      grant-type: client_credentials
      scope: server

server.port: 5100
# management.port: 5101
